# Gossip Glomers

Solutions to a distributed systems challenges(6 challenge) named Gossip Glomers at https://fly.io/dist-sys/

# Setup
The project includes a [devenv.nix](https://devenv.sh/) file along with a [direnv](https://direnv.net/)file, which has all the dependencies.

